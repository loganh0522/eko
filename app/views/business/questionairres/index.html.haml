.contain
  .sidebar
    .nav
      = render 'business/shared/post_job'

  .main-container
    .header   
      %h1 Application Form
    .body
      .not-present-container
        %p There currently is no application form for this job. 
        = link_to "Create Application Form", new_business_job_questionairre_path(@job.id), class: "add-object"