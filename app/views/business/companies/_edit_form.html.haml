= form_for [:business, @company], remote: true do |f|
  .modal-body
    .form-row-no-border
      = f.label :name, "Company Name"
      = f.text_field :name, label: "Company Name", id: "name_Block"
      = f.label :address, "Company Address"
      = f.text_field :address, id: 'geocomplete2'
      %span#name_msgBlock
      = f.label :name, "Company Name"
      = f.text_field :website, label: "Website"     
      .company-kind
        = f.radio_button :kind, "company", id: 'company', checked: true
        = f.label :kind, "Company", :value => "company", :for => 'company'

        = f.radio_button :kind, 'recruiter', id: "recruiter"
        = f.label :kind, "Recruiter", :value => "recruiter", :for => 'recruiter'
  .modal-footer
    = f.button "Update", class: "btn submit-button", data: {disable_with: "<i class='fa fa-spinner fa-spin'> </i> Saving..."}
    .btn.border-button.close-modal Cancel