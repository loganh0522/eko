#all-candidates
  %h1 Clients
  .actions
    .search-field
      = form_tag business_applications_path, method: :get do   
        .form-group
          = search_field_tag :query, params[:query], placeholder: "Advanced Text Search", class: "form-control"
    = link_to "Create Client", new_business_client_path, class: "btn border-button2", id: "move-applicants", remote: true

#job-applicants
  = render 'modal'
  .side-container
    
  .main-container
    .applicants
      = render 'clients_table'

