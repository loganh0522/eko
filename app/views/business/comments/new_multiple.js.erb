$('.multi-form').find('form').remove()
$('.multi-form').find('.modal-title').html('<%= j "Add Comment" %>');
$('.multi-form').find('.modal-header').after('<%= j render "business/comments/form_multi" %>');
$('.multi-form').modal("toggle");