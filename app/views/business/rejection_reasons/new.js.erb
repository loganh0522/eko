$('.multi-form').find('form').remove()
$('.multi-form').find('.modal-title').html('<%= j "Create Rejection Reason" %>');
$('.multi-form').find('.modal-header').after('<%= j render "business/rejection_reasons/form" %>');
$('.multi-form').modal("toggle");