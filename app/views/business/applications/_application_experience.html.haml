- @positions.each do |position| 
  .position
    .job-header
      %h4.title=  "#{position.company_name}"
      %p.date June 2015 - Present
    .job-body       
      %h5= "#{position.title}"
      %p= position.description