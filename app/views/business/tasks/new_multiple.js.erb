$('.multi-form').find('form').remove()
$('.multi-form').find('.modal-title').html('<%= j "Create Task" %>');
$('.multi-form').find('.modal-header').after('<%= j render "business/tasks/form_multiple" %>');
$('.multi-form').modal("toggle");