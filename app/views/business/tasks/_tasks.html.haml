.job-activity-feed-container
  #job-activity-container
    .main-container
      .activity-feed
        - if @tasks.present?
          - @tasks.each do |comment|
            = render 'business/tasks/comment', comment: comment
        - else  
          .not-present-container
            %p There are currently no tasks.
