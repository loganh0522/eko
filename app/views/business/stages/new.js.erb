$('.newModal').find('form').remove();
$('.newModal').find('.new-title').html('<%= j "Create Stage" %>');
$('.newModal').find('.new-modal-header').after('<%= j render "business/stages/form"%>');
$('.newModal').show();