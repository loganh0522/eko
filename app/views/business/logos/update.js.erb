<% if @logo.new_record? %>
  alert("Failed to upload painting: <%= j @logo.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#logo-area").html("<%= j render 'business/logos/form', logo: @logo %>");
<% end %>