<% if @background.new_record? %>
  alert("Failed to upload painting: <%= j @background.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $("#background-area").html("<%= j render 'business/background_images/form', background: @background %>");
<% end %>