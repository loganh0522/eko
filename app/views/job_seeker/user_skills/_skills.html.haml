.title Skills Used: 
- if position.user_skills.present?
  - position.user_skills.each do |skill|
    = render 'job_seeker/user_skills/skill', skill: skill, position: position
- else
  .name No skills listed
