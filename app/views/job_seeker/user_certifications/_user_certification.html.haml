%div{:class => "work-experience", id: "user_certification_#{certification.id}"}
  .job-position
    .experience-header 
      .company= certification.certification.name
      .edit
        = link_to "Edit", edit_job_seeker_user_certification_path(certification.id), :data => {:remote => true} 
    .experience-position
      .title= certification.agency
      .date= "#{certification.start_month} #{certification.start_year} - #{certification.end_month} #{certification.end_year}"    
    .experience-body 
      %p.description= certification.description