!!!5
%html(lang='en-US')
  %body
    %p= simple_format(Liquid::Template.parse(@message.body).render('recipient' => @recipient, 'job' => @job, 'company' => @current_company))
    
    %p= link_to "Schedule Interview Link", register_with_token_url(@invitation.token)